public class parser {
    // Main parse function
    
    public parseTree parse() {

        // Replace with the real parse tree that will be sent to semantic analysis
        return new parseTree();
    }

    // Add a function to parse each non-terminal in the grammar

}
